// import {
//     validateEmail,
//     validateName,
//     validatePassword,
//     validateUserName,
//   } from "../utils/handleRegex";

// export function handleValidateName(name: string) {
//     const containSpeacialCharacter = !validateName.test(name);
//     const isValidLength = name.length >= 3;
//     return isValidLength && !containSpeacialCharacter;
// }

// export function handleValidateUserName(userName: string) {
//     const containSpeacialCharacter = !validateUserName.test(userName);
//     const isValidLength = userName.length >= 3;
//     return isValidLength && !containSpeacialCharacter;
// }

// export function handleValidateEmail(email: string) {
//     const containSpeacialCharacter = !validateEmail.test(email);
//     const isValidLength = email.length >= 1;
//     return isValidLength && !containSpeacialCharacter;
// }

// export function handleValidatePassword(password: string) {
//     const containSpeacialCharacter = !validatePassword.test(password);
//     const isValidLength = password.length >= 8;
//     return isValidLength && !containSpeacialCharacter;
// }
